for (let i = 0; i < 20; i++) {
    if (i % 2 == 0) {
        document.write(`${i} adalah bilangan Genap`);
        document.write("</br>");
    } else {
        document.write(`${i} adalah bilangan Ganjil`);
        document.write("</br>");
    }
}
